[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Serves the Ember.js admin dashboard via Nginx
startCommand = "nginx -g 'daemon off;'"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
